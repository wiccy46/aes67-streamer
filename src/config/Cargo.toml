[package]
name = "config"
version = "0.1.0"
edition = "2021"

[lib]
name = "config"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0"
clap = "4.5.40"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
